# <주제>

<이 파일이 다루는 범위를 한 줄로. 예: "인증 흐름 관련 결정을 모아둔 문서">

## <결정 제목>

- 상태: accepted
- 기록일: YYYY-MM-DD

**결정**: 무엇을 하기로 했는지 한두 문장으로.

**근거**:

- 이 선택이 대안보다 나은 이유
- 관련 제약(백엔드 스펙, 플랫폼 제약 등)이 있으면 같이 적는다

## <다른 결정이 있으면 같은 형식으로 추가>

- 상태: accepted
- 기록일: YYYY-MM-DD

**결정**: ...

**근거**:

- ...

<!--
기존 결정을 나중에 뒤집거나 보완할 때는 위 섹션을 지우지 말고, 그 섹션의 `상태`를
`superseded`로 바꾼 뒤(대체하는 아래 섹션을 `- 대체: <섹션 제목>`으로 링크), 아래처럼
날짜 붙인 새 섹션을 이어서 추가한다. 문제 상황이 있었으면 문제/해결로 나눠 적는다.
-->

## <변경된 결정 제목> — <핵심 변경 요약> (YYYY-MM)

- 상태: accepted
- 기록일: YYYY-MM-DD

**문제**: 기존 결정을 그대로 뒀을 때 실제로 어떤 문제가 발생했는지.

**해결**: 무엇을 바꿨는지, 관련 파일/함수가 있으면 코드 식별자로 명시.

**⚠️ 주의 (선택)**: 이 결정이 특정 전제(플랫폼, 백엔드 동작 등)에 의존한다면, 그 전제가
깨졌을 때의 폴백이나 관련 문서 링크를 남긴다.

<!--
상태 값: proposed(제안됨, 아직 미확정) / accepted(채택되어 현재 유효) /
superseded(다른 섹션으로 대체됨, 대체 섹션 링크 필수) / rejected(검토 후 미채택,
근거를 **근거**에 남긴다).
-->
